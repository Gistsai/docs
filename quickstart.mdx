---
title: 'Tutorial'
---

Learn how to create a trustworthy LLM prompt in 10 minutes. 

## Overview of steps
1. Sign up
1. Create a gist
1. Modify the default variant
1. Enter the prompt with variables
1. Run the gist
1. Add a test case
1. Turn on evaluators
1. Benchmark
1. Check results

## Detailed steps

<AccordionGroup>
  <Accordion title="1. Sign In" defaultOpen>
    Create a free Gists account on the [Sign In](https://app.gists.ai/sign_in) page if you haven't already.
  </Accordion>
  <Accordion title="2. Create a gist" defaultOpen>
    1. Click on `Gists` on the left menu
    2. Click on `New Gist` button on the page
    3. Accept the default name or give it a custom name in the modal
  </Accordion>
  <Accordion title="3. Modify the default variant" defaultOpen>
    
    1. Click on `Gists` on the left menu
    2. Click on `New Gist` button on the page
    3. Accept the default gist name or give it a custom name in the modal
    <Tip>All gists are versioned so you can create new versions without breaking existing behavior.</Tip>
  </Accordion>
  <Accordion title="4. Create a gist" defaultOpen>
    1. Click on `Gists` on the left menu
    2. Click on `New Gist` button on the page
    3. Accept the default gist name or give it a custom name in the modal
  </Accordion>
  <Accordion title="5. Create a gist" defaultOpen>
    1. Click on `Gists` on the left menu
    2. Click on `New Gist` button on the page
    3. Accept the default gist name or give it a custom name in the modal
  </Accordion>
</AccordionGroup>

## What's next?

Add content directly in your files with MDX syntax and React components. You can use any of our components, or even build your own.

<CardGroup>

<Card title="Style Your Docs" icon="paintbrush" href="/settings/global">
  Add flair to your docs with personalized branding.
</Card>

<Card
  title="Add API Endpoints"
  icon="square-code"
  href="/api-playground/configuration"
>
  Implement your OpenAPI spec and enable API user interaction.
</Card>

<Card
  title="Integrate Analytics"
  icon="chart-mixed"
  href="/analytics/supported-integrations"
>
  Draw insights from user interactions with your documentation.
</Card>

<Card
  title="Host on a Custom Domain"
  icon="browser"
  href="/settings/custom-domain/subdomain"
>
  Keep your docs on your own website's subdomain.
</Card>

</CardGroup>
